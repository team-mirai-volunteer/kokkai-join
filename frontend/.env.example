# PostgreSQL Database URL
# Local development:
DATABASE_URL="postgresql://kokkai_user:kokkai_password@localhost:5432/kokkai_db?schema=public"

# Cloud Run with Cloud SQL (Unix socket - recommended):
# DATABASE_URL="postgresql://USERNAME:PASSWORD@/DATABASE_NAME?host=/cloudsql/PROJECT_ID:REGION:INSTANCE_NAME&schema=public&connection_limit=20&pool_timeout=10"
# Example:
# DATABASE_URL="postgresql://kokkai_user:kokkai_password@/kokkai_db?host=/cloudsql/my-project:asia-northeast1:kokkai-instance&schema=public&connection_limit=20&pool_timeout=10"

# Cloud Run with Cloud SQL (Private IP):
# DATABASE_URL="postgresql://USERNAME:PASSWORD@PRIVATE_IP:5432/DATABASE_NAME?schema=public"

# Next.js Environment
# Options: development, production, test
NODE_ENV=production

# Application Port (optional, defaults to 3000)
PORT=3000

# Hostname (optional, defaults to 0.0.0.0 in Docker)
HOSTNAME=0.0.0.0